package com.vibecode.submission.entity;

public enum SubmissionStatus {
    pending,
    running,
    accepted,
    wrong_answer,
    time_limit_exceeded,
    memory_limit_exceeded,
    runtime_error,
    compilation_error
}

